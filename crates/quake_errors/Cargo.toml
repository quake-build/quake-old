[package]
name = "quake_errors"
version = "0.1.0"

license.workspace = true
edition.workspace = true

[dependencies]
miette = "7.2.0"
nu-protocol.workspace = true
thiserror = "1.0.58"

[lints]
workspace = true

[dev-dependencies]
anstream = { version = "0.6.13", features = ["test"] }
